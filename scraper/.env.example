# Supabase project URL (find in Project Settings → API)
SUPABASE_URL=https://hygjskvfkucuwtcihbiw.supabase.co

# Service role key — grants full DB access, bypasses RLS.
# Find in Project Settings → API → "service_role" key.
# DO NOT commit this to git. Keep it local only.
SUPABASE_SERVICE_KEY=your-service-role-key-here
